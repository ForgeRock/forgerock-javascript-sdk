import { sharedNetworkUtils } from './shared-network-utils';

describe('sharedNetworkUtils', () => {
  it('should work', () => {
    expect(sharedNetworkUtils()).toEqual('shared-network-utils');
  });
});
