{
  "name": "ping-protect",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "packages/ping-protect/src",
  "projectType": "library",
  "tags": ["scope:package"],
  "targets": {
    "docs": {
      "command": "npx typedoc --options ./packages/ping-protect/typedoc.json"
    },
    "build": {
      "dependsOn": ["^build"],
      "inputs": [
        "default",
        "^default",
        {
          "externalDependencies": ["vite"]
        }
      ],
      "outputs": ["{options.outDir}"],
      "options": {
        "outDir": "packages/ping-protect/dist"
      }
    },
    "lint": {
      "options": {
        "args": ["./**/*.ts"],
        "fix": true,
        "ignore-path": ".eslintignore"
      }
    },
    "test": {
      "dependsOn": ["^test"],
      "inputs": [
        "default",
        "^default",
        {
          "externalDependencies": ["vitest"]
        }
      ],
      "options": {
        "config": "./vite.config.ts",
        "coverage": true
      }
    }
  }
}
