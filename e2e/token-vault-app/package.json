{
  "name": "token-vault-app",
  "version": "0.0.0",
  "description": "ForgeRock Token Vault Test App",
  "private": true,
  "type": "module",
  "nx": {
    "implicitDependencies": [
      "@forgerock/javascript-sdk",
      "@forgerock/token-vault"
    ],
    "tags": ["scope:app"]
  },
  "scripts": {
    "build": "pnpm build-interceptor && vite build",
    "build-interceptor": "vite build --config ./vite.interceptor.config.ts",
    "serve": "vite dev",
    "preview": "vite preview",
    "lint": "eslint **/*.ts --ignore-path .eslintignore --fix"
  },
  "dependencies": {
    "@forgerock/javascript-sdk": "workspace:*",
    "@forgerock/token-vault": "workspace:*"
  }
}
