{
  "name": "@forgerock/token-vault-proxy",
  "version": "0.0.0",
  "description": "ForgeRock Token Vault Test Proxy",
  "private": true,
  "type": "module",
  "dependencies": {
    "@forgerock/javascript-sdk": "4.6.0",
    "@forgerock/token-vault": "workspace:*"
  },
  "devDependencies": {},
  "scripts": {
    "build": "vite build --mode production",
    "build:dev": "vite build --mode development",
    "start": "vite --host localhost --port 5833",
    "preview": "vite preview --host localhost --port 5833",
    "lint": "eslint --fix --ignore-path .eslintignore \"src/**/*.ts\""
  }
}
